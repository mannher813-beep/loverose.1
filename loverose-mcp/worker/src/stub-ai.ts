/**
 * Stub du package "ai" (Vercel AI SDK).
 * Le package `agents` l'importe dynamiquement uniquement pour ses helpers
 * `aiSdkTools` — fonctionnalité que LoveRose MCP n'utilise pas (outils zod).
 * L'alias wrangler pointe "ai" vers ce stub pour que le bundling réussisse
 * sans embarquer le SDK Vercel AI.
 */
export const jsonSchema = undefined;
export default undefined;
